sync:
  toHost:
    ingresses:
      enabled: true
    gatewayApi:
      httpRoutes:
        enabled: true
  fromHost:
    gateways:
      enabled: true
      mappings:
        byName:
          "nginx-gateway/nginx-gateway": "nginx-gateway/nginx-gateway"